name: Product Contract

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  verity:
    uses: Jason-Furr/verity-spec/.github/workflows/product-contract.yml@v0.75.0
    with:
      workspace: .
